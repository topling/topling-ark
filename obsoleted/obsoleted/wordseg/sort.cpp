/* vim: set tabstop=4 : */
#include "sort.hpp"

/**
 * @file sort.hpp
 * 逐字排序，先排数组中第一个字符，然后第二个……
 * 类似基数排序，但是基数排序是先排最低位，后排最高位
 */

namespace terark { namespace wordseg {

#ifdef _STR_CHAR_POS_SORTER_PRINT_PARAM

std::map<std::string, int> m_nth_file;

#endif

} }

